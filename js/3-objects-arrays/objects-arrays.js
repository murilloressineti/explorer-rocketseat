//Objects
//Revisando a criação de um objeto:

	const person = {
		name: 'John',
		age: 30,
		weight: 88.6,
		isAdmin: true
		}

//Para imprimir apenas uma propriedade desse objeto na tela:

console.log(person.name) 
console.log(person.age)

console.log(`${person.name} tem ${person.age} anos`)

//Arrays
//Revisando a criação de um arrays:

	const animals = [
		'Lion',
		'Monkey',
		'Cat',
		]

//Para acessar valores dentro de um array, deve-se acessar a posição do array:

	console.log(animals[0])
    console.log(animals[1])
	

//Para saber o total de elementos do array:

	console.log(animals.length)
